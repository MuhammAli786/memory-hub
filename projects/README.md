# Projects

This directory contains reusable project operating documents, distinct from
`wiki/projects/`, which contains sourced knowledge pages about actual work.

Use one project document per system or delivery stream. It should define goal,
scope, inputs, outputs, owners, automation, risks, and acceptance checks. Do
not place user history, credentials, raw exports, or service configuration here.
