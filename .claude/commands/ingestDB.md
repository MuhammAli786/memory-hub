---
description: Curate raw sources into the wiki and private knowledge store
argument-hint: "[source folder or limit]"
---

Inspect the requested raw sources. Skip empty, duplicate, and near-empty input before any model call. Search the wiki first. Create only sourced, atomic pages from `wiki/templates/page.md`; update the index and append a factual activity entry. A private adapter may mirror approved page changes to the knowledge container. Never let an automated ingest overwrite a locked human-authored page.
